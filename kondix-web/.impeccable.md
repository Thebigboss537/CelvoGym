# KONDIX Design Context

## Design Context

### Users
**Primary:** Personal trainers in Latin America (25-45, Spanish-speaking). They use desktop or tablet to create workout routines, manage students, and track progress. They value efficiency — building a routine should feel structured but not tedious.

**Secondary:** Students/gym-goers (18-40, Spanish-speaking). They primarily use mobile phones in the gym to follow assigned routines, log sets (weight/reps/RPE), and communicate with their trainer. They need a fast, glanceable UI they can operate between sets with sweaty hands.

**Job to be done:**
- Trainers: "Create structured routines and confidently manage my students' progress"
- Students: "Follow my routine, log my work, and see myself improving"

### Brand Personality
**Three words:** Motivacional. Profesional. Confiable.

**Voice:** Spanish (tú), direct, encouraging — like a good trainer. Short, active sentences. Never condescending or overly formal.

**Emotional goals:**
- **During workout:** Focus, flow, accomplishment
- **Viewing progress:** Pride, motivation to continue
- **Creating routines:** Confidence, efficiency, mastery
- **Empty/waiting states:** Encouragement, not deflation

### Aesthetic Direction

**Visual tone:** Premium dark, energetic but clean. Think Apple Fitness+ meets Hevy meets TrainerRoad.

**Theme:** Dark only — blue-black premium backgrounds (#09090B base) with warm crimson accents (#E62639). The noise texture overlay adds subtle depth. No light mode.

**References:**
- Apple Fitness+: Clean metrics, celebratory completions, premium dark aesthetic
- Hevy: Efficient workout logging, gym-practical mobile UI
- TrainerRoad: Structured training plans, data-driven progress

**Anti-references:**
- Aggressive/edgy gym aesthetics (flames, skulls, extreme contrast)
- Overly colorful/playful (this is a professional tool, not a game)
- Corporate/enterprise feel (keep it personal and warm)

### Design Principles

1. **Progress is visible.** Every interaction should reinforce the feeling of moving forward — progress bars, completion states, and visual feedback make effort tangible.

2. **Gym-practical first.** Students use this between sets with sweaty hands on small screens. Touch targets must be generous, information must be glanceable, and logging must be fast.

3. **Structure without clutter.** Routines have deep hierarchy (Day → Group → Exercise → Set). Show this structure clearly through visual nesting, spacing, and progressive disclosure — never as overwhelming walls of data.

4. **Celebrate achievement.** Set completions, day completions, and routine milestones deserve moments of delight. The glow, the bounce, the pulse — these are earned.

5. **Trust through consistency.** Every button, input, card, and label should feel like it belongs to the same system. Consistent patterns build confidence for both trainers and students.

---

## Technical Context

### Stack
- **Framework:** Angular 21.2.6 (standalone components, signal inputs, inline templates)
- **Styling:** Tailwind CSS 4.2.2 with `@theme` tokens in `src/styles.css`
- **State:** NgRx SignalStore + component-level signals
- **Icons:** Lucide (to be installed — outline style, 1.5px stroke)
- **Fonts:** Syne (display, 600-800) + Outfit (body, 300-700) via Google Fonts
- **No component library** — all UI is custom Tailwind

### Color Tokens
```
Primary:    #E62639 (KONDIX Crimson)
Hover:      #CF2233
Active:     #B31D2C
Subtle bg:  #2A0F14
Glow:       rgba(230, 38, 57, 0.15)

Background: #09090B (base), #111113 (raised), #16161A (card), #1E1E24 (card-hover)
Text:       #F4F4F5 (primary), #A1A1AA (secondary), #71717A (muted)
Border:     #27272A (default), #1E1E22 (light)

Success:    #22C55E / #0D3320
Warning:    #F59E0B / #3D2800
Danger:     #EF4444
```

### Typography
- Display headings: `font-[var(--font-display)]` (Syne)
- Body/UI text: default (Outfit, set on body)
- Scale: 12px (caption) → 14px (body-sm) → 16px (body) → 18px (h3) → 20px (h2) → 24px (h1) → 32px (display)

### Motion
- Entrance: `fade-up` 400ms ease-out
- Completion: `check-bounce` 300ms spring + `pulse-glow` 2s infinite
- Lists: `stagger` cascade (60ms increments)
- Interactive: `press` scale(0.96) on active
- Transitions: 100-500ms range

### Layout Constraints
- Trainer pages: `max-w-2xl` container
- Student pages: `max-w-lg` container (mobile-optimized)
- Forms: `max-w-sm` centered
- Header: `sticky top-0 z-50 h-14 backdrop-blur-md`
- Language: Spanish (es), all UI copy in Spanish

### Component Patterns
- Inputs: `w-full bg-card border border-border rounded-lg px-4 py-3 text-text focus:outline-none focus:border-primary transition`
- Buttons: `bg-primary hover:bg-primary-hover text-white font-semibold py-3 rounded-lg transition press`
- Cards: `bg-card hover:bg-card-hover border border-border rounded-xl p-4 transition press`
- Spinner: `w-8 h-8 border-2 border-primary border-t-transparent rounded-full animate-spin`
- Error: `text-danger text-sm`
- Empty state: Plain text (to be improved)

### Accessibility Targets
- WCAG AA minimum for all text/background combinations
- `prefers-reduced-motion` should be respected (to implement)
- Touch targets: minimum 44px for gym-context mobile use
- Focus-visible rings for keyboard navigation (to implement)

---

## v2 Component Inventory (Phases 1-5, 2026-04)

All standalone, single-file (template + class in `.ts`), `ChangeDetectionStrategy.OnPush`, signal inputs (`input()`/`input.required<T>()`), Lucide icons via `LucideAngularModule` + `LUCIDE_ICONS` provider. Component class names omit the `Component` suffix.

| Component | Sizes / states | Signal API | Animations |
|---|---|---|---|
| `<kx-exercise-thumb>` | `xs` 32 / `sm` 40 / `md` 56 / `lg` 72 / `fill` (`w-full aspect-square`) | `input.required<string>('name')`, `input<string\|null>('muscleGroup')`, `input<string\|null>('photoUrl')`, `input<string>('size','md')`, `input<boolean>('hasVideo',false)` | none |
| `<kx-video-demo-overlay>` | full-screen modal | `input.required<string>('url')`, `input.required<string>('exerciseName')`, `input<boolean>('open',false)`; `output<void>('close')` | backdrop fade-in; iframe injected only while `open()` |
| `<kx-mood-picker>` | inline 4-option (Great/Good/Ok/Tough) | `input<MoodType\|null>('value')`; `output<MoodType>('valueChange')` | press scale on tap |
| `<kx-rpe-stepper>` | 1–10 scale, color-graded green→amber→red | `input<number\|null>('value')`, `input<boolean>('showLabel',true)`; `output<number>('valueChange')` | active button glow |
| `<kx-set-chip>` | compact pill | `input.required<string>('weight')`, `input.required<number>('reps')`, `input<boolean>('isPR',false)`, `input<string\|null>('note',null)`, `input<string>('setType','Effective')` | PR badge `animate-badge` |
| `<kx-session-row>` | expandable timeline row | `input.required<TrainerSessionDto>('session')`, `input<boolean>('expanded',false)`; `output<void>('toggle')` | `.collapse-content` + `.expanded` parent CSS-grid trick |
| `<kx-exercise-feedback-modal>` | centered modal | `input.required<string>('exerciseName')`, `input<boolean>('open',false)`; `output<{rpe:number;notes:string\|null}>('submit')`, `output<void>('skip')` | backdrop fade-in |
| `<kx-recovery-banner>` | full-width amber banner | `input.required<RecoverableSessionDto>('missedSession')`; `output<void>('recover')`, `output<void>('dismiss')` | `animate-fade-up` on appear |

### Component patterns added in v2

- **`<kx-set-row>`** gained `note: string \| null = null`, `showNoteToggle: boolean = false` inputs and `noteChange: string` output. The 💬 toggle reveals an inline textarea via the `.collapse-content` + `.expanded` parent CSS-grid trick (height:auto transition).
- **`<kx-day-cell>`** gained the `'recovered'` state — visual is amber (border + dot + number + corner `rotate-ccw` Lucide icon). Reinforces the warning palette of `<kx-recovery-banner>`.
- **`<kx-toast>`** gained the `'pr'` type + `ToastService.showPR(name, weight, reps)` method (4s, gold/crimson gradient, 🏆 icon, optional `navigator.vibrate([100,60,100,60,200])` when feature-detected).
- **PR badge** on `<kx-set-chip>` uses `animate-badge` (single bounce). Earned, not decorative.

### v2 Tokens

- **Muscle-group tints** for `<kx-exercise-thumb>` fallback gradient (12% opacity over `#0a0a0b`):
  `--color-muscle-pecho`, `--color-muscle-espalda`, `--color-muscle-hombro`, `--color-muscle-biceps`, `--color-muscle-triceps`, `--color-muscle-cuadriceps`, `--color-muscle-femoral`, `--color-muscle-gluteo`, `--color-muscle-gemelo`, `--color-muscle-core`, `--color-muscle-antebrazo`.
- **Set-type colors** (Phase 1 carryover, used by `<kx-set-chip>`): `--color-set-warmup`, `--color-set-effective`, `--color-set-dropset`, `--color-set-restpause`, `--color-set-amrap`.

### v2 Utility surfaces

- `shared/utils/youtube.ts` — `youtubeEmbedUrl(url): SafeResourceUrl`. Used by `<kx-video-demo-overlay>` and the student logging screen. Accepts `youtu.be/`, `youtube.com/watch?v=`, `youtube.com/shorts/` URLs (incl. with leading query params).
- `@angular/cdk/drag-drop` (`DragDropModule`) — only imported in the lazy `program-form` chunk. Used for the trainer's weekly planning grid in `program-form.ts`. Sidebar is one-way: `[cdkDropListSortingDisabled]="true"`, cells' `connectedTo` excludes the sidebar id.

### v2 Notes that aren't components

- The Programs editor's weekly grid is a **planning visualization**, not the routine source-of-truth. The pre-existing rotation-slots editor still owns `ProgramRoutine[]` persistence; the grid + per-week `notes` input round-trip via `PUT /api/v1/programs/{id}/week-overrides/{weekIndex}` only.
- Empty/whitespace `notes` deletes the row server-side (handler-level convention, not a UI gate).
- The student "Recuperar" flow uses `RecoversPlannedDate: DateOnly?` on `StartSessionCommand`, not a session id — there's no `WorkoutSession` for a missed-but-never-started day.

## Programs v3 component family (Phase 4-6)

Trainer-facing program editor + list. Replaces the v2 `program-form.ts` rotation/fixed-grid editor entirely. Source of truth for visuals: `design_handoff_kondix_v2/prototypes/trainer/view-programs.jsx`.

| Component | Visual role | Inputs / outputs | Notes |
|---|---|---|---|
| `<kx-program-card>` | List card with cover gradient, timeline preview, badges (Objective + BORRADOR + EN BUCLE), 3-stat grid, menu (Asignar / Duplicar / Eliminar) | `input.required<ProgramSummary>('program')`; `output<void>('open' \| 'assign' \| 'duplicate' \| 'delete')` | Used by `program-list.ts`. Menu uses `$event.stopPropagation()` so menu clicks don't fire `(open)`. |
| `<kx-program-meta-panel>` | Editor LEFT column (name, description, Objetivo, Nivel, Mode toggle, Notas, footer counts) | `input.required<ProgramDetail>('program')`; `output<MetaPatch>('patch')`, `output<ProgramMode>('modeChange')` | Text inputs debounce on blur (not per-keystroke) to avoid 14-PUTs-per-rename. Selects + mode toggle emit immediately. |
| `<kx-program-week-row>` | Editor CENTER row: label + N day-cells + duplicate/delete menu | `input.required<ProgramWeek>('week')`, `input<number\|null>('selectedDayIndex')`, `input<boolean>('hideMenu')`, `input<boolean>('canDelete')`, `input<string\|null>('programObjective')`; `output<number>('selectCell')`, `output<void>('duplicate' \| 'delete')` | Menu hidden in Loop mode (`hideMenu=true`). Empty `<div></div>` placeholder keeps grid columns aligned. |
| `<kx-program-day-cell>` | One slot in the week, 3 visual states (Empty / Rest / RoutineDay) | `input.required<ProgramSlot>('slot')`, `input<string\|null>('category')`, `input<boolean>('isSelected')`; `output<void>('select')` | RoutineDay state uses objective accent via `[ngStyle]`. |
| `<kx-cell-inspector>` | Editor RIGHT inspector for the selected cell | `input<ProgramSlot\|null>('slot')`, `input<number\|null>('weekIndex')`, `input<string\|null>('dayLabel')`, `input<boolean>('canMarkRest')`; `output<'Empty'\|'Rest'>('setKind')`, `output<void>('assign')`, `output<string>('removeBlock')` | 4 view states: null placeholder, Empty, Rest, RoutineDay (with blockId vs no-blockId branch). The "Progresión" overrides section is intentionally dropped (Q6 closed for v3). |
| `<kx-create-program-modal>` | Creation modal | `output<void>('close')`, `output<{id:string}>('created')` | One-shot at the program list. POSTs to `/programs`. |
| `<kx-assign-routine-modal>` | 2-step wizard (picker + scope+mapping) | `input.required<ProgramDetail>('program')`, `input<number>('initialWeek',0)`; `output<void>('close')`, `output<{routineId,weeks,mapping?,dayIds?}>('assigned')` | Picker fetches `GET /routines` (flat shape, no `days[]`); on `pick(r)` fetches `GET /routines/{id}` for the days. Per-row loader during detail fetch. Auto-suggested mapping uses `suggestWeekdayMapping([0,2,4,1,3,5,6])` — Mon/Wed/Fri then Tue/Thu/Sat/Sun. |
| `<kx-assign-program-modal>` | Minimal assign-to-student modal (student select + start date) | `input.required<string>('programId' \| 'programName')`; `output<void>('close' \| 'assigned')` | Server-side: assigning a published program cancels the student's prior active assignment. Trainer cannot assign unpublished programs (handler throws "Publicá el programa primero"). |

### v3 utility surfaces

- `shared/utils/objective-color.ts` — `objectiveColor(o: ProgramObjective): string`. Hex map for the 6 objectives (Crimson / Amber / Green / Blue / Purple / Gray). Used by `<kx-program-card>` and friends.
- `features/trainer/programs/utils/weekday-mapping.ts` — `suggestWeekdayMapping(dayIds): Record<id, weekdayIdx | null>` with the L-V spread pattern. Used by `<kx-assign-routine-modal>` when scheduleType=Week.
- `features/trainer/programs/data-access/program-editor.store.ts` — `ProgramEditorStore` (NgRx SignalStore, locally provided). Reload + selectCell/clearSelection + 9 mutation methods. After every mutation reloads the full ProgramDetail.

### v3 Tailwind class gotchas

- The v3 plan referenced `btn-primary`/`btn-outline`/`btn-ghost`/`btn-sm`/`class="input"`/`bg-primary-subtle`/`scroll-thin`/`bg-bg-deep`. **They do not exist** in `styles.css`. Inline the equivalent utility classes — `assign-routine-modal.ts` and `assign-program-modal.ts` show the canonical patterns. Token `--color-primary-light: #2A0F14` IS defined and is the active-state subtle background.
- `var(--color-X)55` (hex-alpha on a CSS variable) is invalid CSS. Use `color-mix(in srgb, var(--color-X) 33%, transparent)` instead. Browsers we target all support it.
- `[(ngModel)]` does NOT bind directly to signals. Use `[ngModel]="signal()" (ngModelChange)="signal.set($event)"` instead.
